<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinApp.Views.DetalhesView">
    
    <StackLayout Padding="25">
        <TableView Intent="Settings">
            <TableRoot>
                <TableSection Title="Acessórios">
                    <SwitchCell Text="{Binding TextoFreioABS}" On="{Binding TemFreioABS, Mode=TwoWay}"></SwitchCell>
                    <SwitchCell Text="{Binding TextoArCondicionado}" On="{Binding TemArCondicionado, Mode=TwoWay}"></SwitchCell>
                    <SwitchCell Text="{Binding TextoDispositivoMultimidia}" On="{Binding TemDispositivoMultimidia, Mode=TwoWay}"></SwitchCell>
                    <TextCell Text="{Binding ValorTotal, Mode=TwoWay}"></TextCell>
                </TableSection>
            </TableRoot>
        </TableView>
        
        <Button 
            x:Name="btnProximo" 
            Text="Próximo"
            Clicked="BtnProximo_Clicked"
            VerticalOptions="End" />
    </StackLayout>
    
</ContentPage>